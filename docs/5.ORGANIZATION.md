# ⚙️ Organization

This guide summarizes repository and runtime data organization.

## Directory Structure

```text
myastroboard/
├── backend/
│   ├── app.py                          ← Flask application factory/entry point (settings, extensions, blueprint registration, startup)
│   │
│   ├── utils/                          ← Cross-cutting support modules (config, i18n, logging, auth, generic helpers)
│   │   ├── __init__.py                 ← Generic JSON file helpers (former utils.py)
│   │   ├── route_helpers.py            ← Shared cross-domain route helpers (active-location resolution)
│   │   ├── app_settings.py             ← Persistent app settings (VAPID email, proxy headers…)
│   │   ├── auth.py                     ← Authentication, sessions, user roles
│   │   ├── config_defaults.py          ← Default configuration constants
│   │   ├── constants.py                ← All TTL constants, file paths, URLs
│   │   ├── events_aggregator.py        ← Merged upcoming-events feed
│   │   ├── i18n_utils.py               ← i18n translation helpers (server-side)
│   │   ├── logging_config.py           ← Centralized logging setup
│   │   ├── metrics_collector.py        ← System metrics collection
│   │   ├── on_demand_translate.py      ← On-demand DeepL/LibreTranslate integration
│   │   ├── push_manager.py             ← VAPID key management, Web Push send wrapper
│   │   ├── push_scheduler.py           ← Push notification scheduler (N1–N7 trigger evaluation)
│   │   ├── repo_config.py              ← config.json load/save
│   │   ├── txtconf_loader.py           ← Legacy text-format config loader (migration only)
│   │   └── version_checker.py          ← GitHub release version comparison
│   │
│   ├── blueprints/                     ← HTTP routes, grouped by domain (Flask Blueprints)
│   │   ├── auth.py                     ← /api/auth/*, /api/users/*
│   │   ├── push.py                     ← /api/push/*
│   │   ├── locations.py                ← /api/config, /api/locations/*
│   │   ├── connectors.py               ← /api/connectors/* (AllSky)
│   │   ├── admin.py                    ← /api/admin/*, /api/metrics, /api/backup/*, /api/logs/*
│   │   ├── misc.py                     ← /api/skyquality, /api/convert-coordinates, /api/timezones, /api/health, /api/cache, /api/version
│   │   ├── weather.py                  ← /api/weather/*, /api/moon/*, /api/aurora/predictions, /api/seeing-forecast
│   │   ├── tracking.py                 ← /api/object/*, /api/iss/*, /api/css/*, /api/spaceflight/*, /api/translate/on-demand
│   │   ├── astronomy.py                ← /api/sky-widget, /api/sun/*, /api/events/*, /api/astro/*, /api/tonight/best-window
│   │   ├── plan_my_night.py            ← /api/plan-my-night/*
│   │   ├── astrodex.py                 ← /api/astrodex/*, /api/beginner-catalog
│   │   ├── equipment.py                ← /api/equipment/*
│   │   └── skytonight_api.py           ← /api/skytonight/*, /api/catalogues (SkyTonight API, still filed under skytonight_* naming)
│   │
│   ├── skytonight/                     ← SkyTonight calculation pipeline (routes live in blueprints/skytonight_api.py)
│   │   ├── skytonight_calculator.py    ← Main SkyTonight observability calculation engine
│   │   ├── skytonight_scheduler.py     ← Scheduler daemon (dawn/dusk timing)
│   │   ├── skytonight_scheduler_manager.py ← Multi-worker scheduler coordination
│   │   ├── skytonight_catalogue_builder.py ← DSO dataset assembly from all catalogues
│   │   ├── skytonight_targets.py       ← Target normalization and catalogue lookup
│   │   ├── skytonight_storage.py       ← JSON output file management
│   │   ├── skytonight_models.py        ← Dataclasses for SkyTonight results
│   │   ├── skytonight_bodies.py        ← Solar-system body calculations (planets, Moon, Sun)
│   │   └── skytonight_comets.py        ← Comet orbital propagation and scoring
│   │
│   ├── astroweather/                   ← Astronomical/atmospheric forecast services
│   │   ├── moon_phases.py                  ← Moon phase, illumination, rise/set, dark window
│   │   ├── moon_planner.py                 ← 7-night moon forecast and month calendar
│   │   ├── moon_eclipse.py                 ← Next lunar eclipse finder and astrophotography score
│   │   ├── moon_astrotonight.py            ← Best imaging window (strict/practical/illumination modes)
│   │   ├── sun_phases.py                   ← Sunrise/sunset, twilight windows (civil/nautical/astronomical)
│   │   ├── sun_eclipse.py                  ← Next solar eclipse finder
│   │   ├── horizon_graph.py                ← Daily sun/moon altitude-vs-time chart data
│   │   ├── aurora_predictions.py           ← NOAA Kp index, geomagnetic forecast, aurora probability
│   │   └── seeing_forecast_7timer.py       ← 7Timer ASTRO seeing and transparency forecast
│   │
│   ├── weather/                        ← Weather forecast clients and astrophotography weather analysis
│   │   ├── sky_quality.py                  ← Light pollution (Bortle/SQM) integration with AstroScore
│   │   ├── weather_openmeteo.py            ← Open-Meteo API client (forecast, rate-limiting)
│   │   ├── weather_astro.py                ← Astrophotography-specific weather metrics (seeing, transparency)
│   │   └── weather_utils.py                ← Cached vs. fresh Open-Meteo client factory
│   │
│   ├── observation/                    ← Observation logbook, planning, and events business logic
│   │   ├── astrodex.py                     ← Astrophotography logbook business logic (CRUD, images, alias matching)
│   │   ├── beginner_catalog.py             ← Curated starter DSO list (loading, i18n, enrichment)
│   │   ├── catalogue_aliases.py            ← Catalogue cross-reference alias table
│   │   ├── object_info.py                  ← Single-object coordinate and catalogue lookup
│   │   ├── plan_my_night.py                ← Night planning timeline (CRUD, CSV/PDF export)
│   │   ├── planetary_events.py             ← Conjunctions, oppositions, elongations, retrograde
│   │   ├── sidereal_time.py                ← Local Apparent Sidereal Time
│   │   ├── solar_system_events.py          ← Meteor shower database and radiant visibility
│   │   └── special_phenomena.py            ← Equinoxes, solstices, zodiacal light, Milky Way visibility
│   │
│   ├── cache/                          ← Shared JSON cache store and background scheduler/updater
│   │   ├── cache_store.py                  ← In-memory + shared JSON cache with per-job TTL
│   │   ├── cache_updater.py                ← Individual cache update functions (selective refresh)
│   │   └── cache_scheduler.py              ← Background cache scheduler daemon
│   │
│   ├── space/                          ← Spaceflight tracking
│   │   ├── iss_passes.py                   ← ISS pass prediction (SGP4), solar/lunar transits, live position
│   │   ├── css_passes.py                   ← CSS (Tiangong, NORAD 48274) pass prediction - full parallel mirror of iss_passes.py
│   │   └── spaceflight_tracker.py          ← Launch Library 2 client (launches, astronauts, events)
│   │
│   ├── equipment/                      ← Equipment profiles business logic
│   │   └── equipment_profiles.py           ← Equipment profiles business logic (telescope/camera/mount/filter/combo), FOV calculator
│
├── data/                               ← Runtime data (Docker volume, persists across updates)
│   ├── config.json                     ← Site configuration (location, constraints, scheduler)
│   ├── users.json                      ← User accounts (hashed passwords, preferences, push subscriptions)
│   ├── app_settings.json               ← Application settings (VAPID email, proxy headers, cookie flags)
│   ├── secret_key.txt                  ← Flask session signing key (generated once, never rotate)
│   ├── vapid.json                      ← Web Push VAPID key pair (generated once, never delete)
│   ├── astrodex/                       ← Astrodex collections (one JSON per user + images/)
│   ├── cache/                          ← Cached astronomical calculations + scheduler lock file
│   ├── equipments/                     ← Equipment profiles (one JSON per user per gear type)
│   ├── logs/                           ← Application log files
│   ├── projects/                       ← Plan My Night files (one JSON per user)
│   └── skytonight/
│       ├── calculations/               ← Current night results (dso/bodies/comets JSON files)
│       ├── catalogues/                 ← Comet elements cache
│       ├── logs/                       ← Per-run SkyTonight calculation logs
│       ├── outputs/                    ← Per-target altitude-vs-time JSON files
│       └── runtime/                    ← Scheduler status JSON
│
├── backend/catalogues/                 ← Static catalogue data files (JSON)
│   ├── pensack500.json                 ← Pensack 500 Best DSO List
│   ├── lbn.json                        ← Lynds' Bright Nebulae cross-references
│   ├── garyimm_crossrefs.json          ← Gary Imm cross-references
│   ├── garyimm_standalone.json         ← Gary Imm standalone objects
│   ├── arp.json                        ← Arp Atlas of Peculiar Galaxies
│   ├── sharpless.json                  ← Sharpless HII emission nebulae (313 objects)
│   ├── barnard.json                    ← Barnard dark nebulae (343 objects)
│   ├── vdb.json                        ← van den Bergh reflection nebulae (158 objects)
│   ├── abell_pne.json                  ← Abell planetary nebulae (71 objects)
│   └── abell_clusters.json             ← Abell galaxy clusters (2 712 objects)
│
├── docs/                               ← Documentation (this directory)
│   ├── README.md                       ← Documentation index (start here)
│   ├── 1.INSTALLATION.md               ← Docker installation guide
│   ├── 2.QUICKSTART.md                 ← 5-minute quick start
│   ├── 3.UPDATE.md                     ← Update procedure
│   ├── 4.RELEASE.md                    ← Release / publishing guide (maintainers)
│   ├── 5.ORGANIZATION.md               ← This file
│   ├── 6.REVERSE_PROXY.md              ← HTTPS / reverse proxy setup
│   ├── 7.TRANSLATIONS.md               ← i18n contribution guide
│   ├── API_ENDPOINTS.md                ← All HTTP routes
│   ├── ASTROPHOTOGRAPHY.md             ← Astrophotography tab: moon, sun, aurora, events, formulas
│   ├── ASTRODEX.md                     ← Astrodex logbook: data model, image management
│   ├── AUTHENTICATION.md               ← Roles, users, preferences, session security
│   ├── CACHE_SYSTEM.md                 ← Cache architecture, TTLs, selective refresh
│   ├── CONFIGURATION.md                ← Location, constraints, backup/restore, logs, metrics
│   ├── EQUIPMENT.md                    ← Equipment profiles, FOV calculator formulas, sharing
│   ├── EXPOSURE_CALC.md                ← Exposure time formula and calibration
│   ├── NOTIFICATIONS.md                ← Browser and Web Push notifications (N1–N7)
│   ├── PLAN_MY_NIGHT.md                ← Night planning workflow
│   ├── SKYTONIGHT.md                   ← AstroScore formula, catalogues, constraints
│   ├── SPACEFLIGHT.md                  ← Launches, ISS passes (SGP4), astronauts
│   ├── VISUAL_TOUR.md                  ← Screenshots and feature walkthrough
│   └── WEATHER.md                      ← Weather system: Open-Meteo, 7Timer, astro-analysis
│
├── scripts/
│   ├── build_skytonight_catalogue.py   ← Build the static DSO dataset JSON
│   ├── minify_static.py                ← Minify CSS/JS assets for production
│   ├── translate_checker.py            ← Check i18n key coverage
│   └── translate_i18n_values.py        ← Bulk-translate i18n files via API
│
├── static/
│   ├── css/                            ← Stylesheets
│   ├── i18n/                           ← Translation JSON files (en.json, fr.json…)
│   ├── img/                            ← Static images
│   ├── js/                             ← Frontend JavaScript modules
│   ├── ico/                            ← PWA icons
│   └── sw.js                           ← Service worker (offline support, Web Push)
│
├── templates/
│   ├── index.html                      ← Main SPA template
│   └── login.html                      ← Login page
│
├── tests/
│   ├── conftest.py
│   ├── test_skytonight_api.py
│   ├── test_skytonight_catalogue_builder.py
│   ├── test_skytonight_targets.py
│   └── ...
│
├── docker-compose.yml
├── docker-compose-dev.yml
├── Dockerfile
├── README.md
└── VERSION
```

## Key data flows

```
Browser request
  └─ app.py (Flask app) → routed to the matching backend/blueprints/*.py Blueprint
       ├─ Authentication check (auth.py)
       ├─ Cache hit? → Return cached JSON (cache_store.py)
       └─ Cache miss (202 Pending) → wait for background scheduler
                 ↑
       cache_scheduler.py (background daemon)
         └─ cache_updater.py (selective, per-job TTL)
               ├─ moon_phases.py, sun_phases.py, aurora_predictions.py…
               └─ weather_openmeteo.py, seeing_forecast_7timer.py…

SkyTonight pipeline (separate scheduler, backend/skytonight/)
  skytonight_scheduler.py
    └─ skytonight_calculator.py
          ├─ skytonight_catalogue_builder.py (DSO dataset)
          ├─ skytonight_bodies.py (planets)
          ├─ skytonight_comets.py (comets)
          └─ Writes JSON → data/skytonight/calculations/
```

## Notes

- SkyTonight is the active target pipeline and scheduler path.
- Dataset generation is handled by `scripts/build_skytonight_catalogue.py`.
- All per-user data (Astrodex, equipment, plans) is stored in `data/` under the user's UUID.
- Never delete `data/secret_key.txt` or `data/vapid.json` — they cannot be regenerated without side effects.
